# SESSION STARTER — paste this to begin every Cowork session

Copy the block below and paste it as your first message in a new Cowork session.

---

```
Connect to /Users/ASUS/Documents/GitHub/experimentation-systems-lab

Run the spine protocol:
  rm -f .git/index.lock .git/HEAD.lock
  git status --short
  npm run build

Then read BRAIN_TRANSFER.md and tell me the current state of the repo before doing anything else. Do not read any other files at open. Grep before reading any source file.
```

---

That's it. BRAIN_TRANSFER.md has everything — current version, what was just done, next action, reusable components, git template. Three files exist for reference if needed: NEXT.md (queue), DECISIONS.md (rules), CLAUDE.md (system prompt, already loaded).
